<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:w="clr-namespace:HandSchool.Views"
             x:Class="HandSchool.Views.SettingPage"
             xmlns:local="clr-namespace:HandSchool.Views"
             Title="{Binding Title}"
             x:Name="ThisPage">
    <ListView x:Name="MyListView"
              ItemsSource="{Binding Items}"
              RowHeight="150"
              ItemSelected="itemseleted">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout>
                        <Label Text="{Binding Title}" FontSize="16" />
                        <w:ValueCell Value="{Binding Value}" Attribute="{Binding AttributeData}" Type="{Binding Type}" />
                        <Label Text="{Binding Description}" FontSize="13" />
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>