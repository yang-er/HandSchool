<local:ViewPage
    x:Class="HandSchool.UWP.MessageDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HandSchool.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="24,0,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock HorizontalAlignment="Left" Margin="335,486,0,0" Grid.Row="3" Text="TextBlock" TextWrapping="Wrap" VerticalAlignment="Top"/>
        <TextBlock FontSize="24" Grid.Row="0" Text="{x:Bind Title}" TextWrapping="Wrap" VerticalAlignment="Top" />
        <TextBlock FontSize="24" Grid.Row="1" Text="{x:Bind Time}" TextWrapping="Wrap" VerticalAlignment="Top" />
        <TextBlock FontSize="24" Grid.Row="2" Text="{x:Bind Sender}" TextWrapping="Wrap" VerticalAlignment="Top" />

    </Grid>
</local:ViewPage>
