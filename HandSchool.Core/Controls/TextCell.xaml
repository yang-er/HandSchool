<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
          xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
          x:Class="HandSchool.Views.TextCell">
    <StackLayout Padding="15">
        <Label Text="{Binding Title}" x:Name="TitleLabel" Style="{DynamicResource ListItemTextStyle}" FontSize="16" />
        <Label Text="{Binding Detail}" x:Name="DescriptionLabel" Style="{DynamicResource ListItemDetailTextStyle}" FontSize="13" />
    </StackLayout>
</ViewCell>