<local:ViewPage
    x:Class="HandSchool.Views.MessageDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HandSchool.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="24,0,24,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock FontSize="24" Grid.Row="0" Margin="0,0,0,8" Text="{x:Bind Title}" TextWrapping="Wrap" VerticalAlignment="Top" />
            <TextBlock FontSize="16" Grid.Row="2" Margin="0,0,0,8" Text="{x:Bind Sender}" TextWrapping="Wrap" VerticalAlignment="Top" />
            <TextBlock FontSize="16" Grid.Row="1" Margin="0,0,0,8" Text="{x:Bind Time}" TextWrapping="Wrap" VerticalAlignment="Top" />
            <TextBlock Margin="0,16,0,8" Grid.Row="3" FontSize="16" Text="{x:Bind Body}" TextWrapping="Wrap" VerticalAlignment="Top" />
        </Grid>
    </ScrollViewer>
</local:ViewPage>
