<UserControl
    x:Class="HandSchool.Views.CardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HandSchool.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Background="{StaticResource CardViewBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Background="{ThemeResource SystemControlBackgroundAccentBrush}" Padding="16">
            <SymbolIcon x:Name="symbolIcon" Foreground="White" />
        </Border>
        <StackPanel x:Name="StackPanel" BorderBrush="{ThemeResource SystemControlBackgroundAccentBrush}" Margin="0,-2,0,0" BorderThickness="2" Padding="16" Grid.Row="1" />
    </Grid>
</UserControl>
